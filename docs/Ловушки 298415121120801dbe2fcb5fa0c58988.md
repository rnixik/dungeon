# Ловушки

### 🎯 Цели ловушек

- Усиливают **напряжение и недоверие** (FF + культист).
- Разнообразят комнаты: требуют **позиционирования**, **тайминга** и **кооперации**.
- Дают культисту пространство для **скрытого саботажа**.

### 🧩 Типы ловушек (MVP)

| Тип | Что делает | Телеграф (честность) | Контр-игра |
| --- | --- | --- | --- |
| 🗡 Шипы (пол) | Всплывают, наносят урон при активации | Анимация плитки + ~~звук~~ за 0.4–0.7 c до удара | Отход по таймингу, ~~Лёд блокирует, Щит спасает~~ |
| 🏹 Стрелы (стена) | Стреляет залпом по прямой | Щель в стене, ~~свист перед выстрелом~~ | ~~Прижаться к стене, барьер Льда, Руна-Щит~~ |
| 🔥 Огонь (пол) | Линия огня, DoT | Разгорание + ~~рычание~~ огня 0.5 c | Обойти дугу, ~~погасить Водой/Льдом~~ |
| ☠️ Ядовитые пары (пол/решётка) | Облако DoT | Зеленоватый туман нарастает 0.6 c | Продув (механизм), ~~Щит защищает частично~~ |

Правило честности: **всегда** есть телеграф (условные знаки) ≥ 0.4 c и читабельный источник (визуально понятно, откуда прилетело).

![](https://img.itch.zone/aW1nLzMyMDA1OTguZ2lm/original/IK2Wu8.gif)

[https://64.media.tumblr.com/ea124946e4f3848a1155d1b0db630fb4/tumblr_oki806jKAv1thw08io1_500.gif](https://64.media.tumblr.com/ea124946e4f3848a1155d1b0db630fb4/tumblr_oki806jKAv1thw08io1_500.gif)

[med (2).webm](med_(2).webm)

### ⚖️ Балансные ручки

- Урон (разовый/DoT), длительность, перерыв (cooldown), размер зоны, телеграф-время.
- ~~Шанс крит/статус-эффекта (горение, замедление, яд).~~
- Вариативность: «синхронная сетка шипов» vs «волны» vs «случайный узор».

### 🤝 Кооп и культист

- Рычаги **в противоположных концах** комнаты → требуется разделение.
- ~~Electromancer активирует механизмы безопасно; Arcanist телепортирует через опасную зону; Cryomancer **замораживает** шипы.~~
- Культист: может **связывать** ловушки (chain) или **подменять** режим (с таймера → по триггеру).

## 🛠 Реализация (система состояний, триггеры, данные)

### 🔁 Состояния ловушки (FSM)

- **Disabled** → **Armed** → **Warning** (телеграф) → **Active** (наносит урон) → **Cooldown** → **Armed**
- Переходы:
    - `Armed → Warning`: при входящем событии триггера.
    - `Warning → Active`: через `warning_time`.
    - `Active → Cooldown`: по `active_time` или пустой цели.
    - `Cooldown → Armed`: по таймеру/сигналу.
    - Любое состояние → `Disabled`: глобальные эффекты (пазл выключил секцию).

### 🧲 Источники активации

- **Всегда включено** (loop): `TimerActivator(period, phase_jitter)`
- **По триггеру**: `LinkActivator(source_id, on_event=[Enter, Exit, Solve, Fail])`
- **По таймеру вкл/выкл**: `ToggleActivator(on_time, off_time, phase)`
- **После выключения связанного**: `ChainActivator(required_state=Disabled)`
- **Тайл-триггер**:
    - `OnAnyPresence` — «кто угодно наступил»
    - `OnSpecificPresence(tag)` — «класс/предмет/вес»
    - `OnAbsence(tag)` — «плита должна быть зажата, иначе ловушка»
    - (опционально) `OnWeight≥N`, `OnCarry(item_id)`, `OnLight(level<threshold)`

### 🧪 Параметры (единая дата-схема)

```yaml
Trap:
  id: string
  type: enum[spikes, arrows, fire, poison]
  element: enum[physical, fire, poison, lightning]   # для резистов
  fsm:
    warning_time: float   # телеграф, с
    active_time: float    # активная фаза, с
    cooldown_time: float  # перерыв, с
  damage:
    hit: int              # разовый урон
    dot: int              # урон/с
    dot_duration: float   # длительность DoT
    status: enum[burn, slow, poison, stun, none]
    status_duration: float
  area:
    shape: enum[circle, box, line, cone]
    size: {r: float, w: float, h: float, len: float, angle: float}
    origin: enum[floor, wall, emitter]
    direction: enum[forward, both, random]
  activators: [ActivatorSpec...] # см. список выше
  links: [trap_id...]            # для цепочек
  telegraph:
    vfx: enum
    sfx: enum
    intensity: float
  fairness:
    min_reaction_time: float     # ≥ 0.4 c по правилу честности
    pre_warn: bool

```

### 🧰 Техническая архитектура (компоненты)

- `TrapController` — держит FSM и параметры.
- `TriggerSource` — события тайла/таймера/линка.
- `DamageArea` — управляет хитбоксами/тик-логикой.
- `VFX/SFX Telegraph` — предсигнал, синк с `warning_time`.
- `LinkBus` — событийная шина (Pub/Sub) между ловушками и рычагами.
- `NetAuthority` — сервер-авторитет на активации; клиенты только предиктят телеграф (для онлайна).

### 🌐 Сеть (мультиплеер)

- Сервер решает: **когда** ловушка активна и **кого** задело.
- Клиенты показывают **телеграф сразу** (prediction), урон — по серверному подтверждению.
- Анти-эксплойт: не принимать клиентские «я не попал в хитбокс» в активной фазе.

## 🧪 Примеры пресетов (готово класть в таблицу данных)

**Spikes_Basic**

- `warning/active/cooldown`: 0.5 / 0.4 / 1.6
- `damage.hit=18`
- `area`: box 1×1 (тайл)
- `activators`: `Timer(period=2.5, jitter=0.2)`

**Arrows_LeftWall_Salvo**

- `warning=0.6`, залп 3 стрелы
- `damage.hit=15`, `status=bleed 4/3c`
- `area`: line len=12 m, width=0.6 m
- `activators`: `OnSpecificPresence(tag=Weight≥2)` на центральной плите

**Fire_Brazier_Cone**

- `warning=0.5, active=1.5, cooldown=2.0`
- `hit=6`, `DoT=3/с ×2c`, статус `burn`
- `area`: cone 60°, len=6 m
- `activators`: `Toggle(on=1.5, off=2.0)`

**Poison_Grate_Cloud**

- `warning=0.6, active=3.0, cooldown=2.5`
- `DoT=4/с ×3c`, статус `poison`
- `area`: circle r=2.5 m
- `activators`: `Chain(required=Disabled of Fire_Brazier_Cone)`